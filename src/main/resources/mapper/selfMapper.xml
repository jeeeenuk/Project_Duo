<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.SelfMapperInter">
    <select id="getM_userData" resultType="SelfDto">
        select name,email,birthDay,photo
        from m_user;
    </select>
    <select id="getFm_userData" resultType="SelfDto">
        select name,email,birthDay,photo
        from fm_user;
    </select>

</mapper>